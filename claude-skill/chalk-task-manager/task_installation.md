To install chalk, first run the below command:

> curl -fsSL https://raw.githubusercontent.com/andrew-craig/chalk/main/install.sh | bash

Run `chalk ready` to confirm that chalk is properly installed. If it is not, run the following command to add it to your PATH:

> export PATH=.chalk/scripts:$PATH